"use client";
export default function BoardStats({board}){
 const columns=board?.columns||[]; const cards=columns.flatMap(c=>c.cards); const doneIds=new Set(columns.filter(c=>/done|complete/i.test(c.name)).flatMap(c=>c.cards.map(x=>String(x._id)))); const done=doneIds.size; const high=cards.filter(c=>c.priority==='high'&&!doneIds.has(String(c._id))).length; const overdue=cards.filter(c=>c.dueDate&&new Date(c.dueDate)<new Date()&&!doneIds.has(String(c._id))).length; const progress=cards.length?Math.round(done/cards.length*100):0;
 const stats=[['Total tasks',cards.length,'▦','text-brand-600 bg-brand-50 dark:bg-brand-500/10'],['Completed',done,'✓','text-emerald-600 bg-emerald-50 dark:bg-emerald-500/10'],['High priority',high,'!','text-amber-600 bg-amber-50 dark:bg-amber-500/10'],['Overdue',overdue,'◷','text-rose-600 bg-rose-50 dark:bg-rose-500/10']];
 return <section className="mb-5 grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{stats.map(([label,value,icon,cls])=><div key={label} className="glass card-shadow rounded-2xl p-4"><div className="flex items-center gap-3"><div className={`grid h-10 w-10 place-items-center rounded-xl font-black ${cls}`}>{icon}</div><div><p className="text-xs font-semibold text-slate-400">{label}</p><p className="text-2xl font-black text-slate-800 dark:text-white">{value}</p></div></div></div>)}<div className="glass card-shadow rounded-2xl p-4 sm:col-span-2 lg:col-span-4"><div className="flex justify-between text-xs font-bold text-slate-500"><span>Overall progress</span><span>{progress}%</span></div><div className="mt-2 h-2.5 rounded-full bg-slate-100 dark:bg-slate-800"><div className="h-full rounded-full bg-gradient-to-r from-brand-600 via-violet-500 to-cyan-500 transition-all duration-700" style={{width:`${progress}%`}}/></div></div></section>
}
